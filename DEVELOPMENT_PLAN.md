# 《琵琶曲》Web 原型开发计划书

版本：1.1  
日期：2026-09-22  
状态：原型开发进行中；P0～P4 已完成；P5 已实现，等待用户验收

## 0. 执行对象与执行协议

本计划书是我（Codex）后续开发《琵琶曲》小游戏时必须遵循的实现规范，不是面向其他开发者的建议文档。用户在后续对话中的最新明确要求优先于本文件；没有新要求时，我应以本文件作为范围、顺序和验收依据。

后续开发必须采用分批执行方式：

- 每次只推进第 8 节中的一个原型开发批次；只有用户明确要求合并时才跨批处理。
- 开始每一批前，先检查项目现状、上一批验收结果和工作区已有改动。
- 每一批只实现其列出的交付内容，同时为后续批次保留清晰接口。
- 每一批结束后运行与风险相称的测试，并向用户汇报：修改文件、完成内容、测试结果、验收项、已知偏差和下一批入口。
- 当前批次通过验收后，再以其结果作为下一批的开发基础。
- 所有临时数值必须进入集中配置；任何偏离本计划的决定都要在交付说明中明确记录，并同步回写本文件。

## 1. 项目目标

制作一个可在桌面浏览器中运行的 2D 横版空战原型，用最小内容量验证以下核心假设：

> 玩家在持续躲避普通弹幕的同时，主动接近并反弹特殊弹来获取能量；再根据局面选择短期强化或积攒全屏大招，形成“承压—冒险—反击—释放”的循环。

原型集中回答四个问题：

- 移动屁股躲避弹幕是否具有清楚、直接的手感。
- 特殊弹能否让玩家主动冒险，而不是只待在安全区。
- 2 点强化与 10 点大招是否构成真实选择，而不是出现唯一最优解。
- 巨大的非碰撞身体能否在不误导玩家的前提下形成鲜明表演。

## 2. 原型范围

### 2.1 原型交付范围

- 一个 60～90 秒的 Boss 战斗房间。
- 一个玩家角色：屁股为唯一受击核心，头部、躯干与四肢只负责表演。
- 普通弹、特殊弹、手动反弹、能量、短期强化和满能量大招。
- 一个三阶段测试 Boss，通过移动速度、发射速度和弹丸速度的变化提供递增压力。
- 3 点生命、受伤无敌、胜利、失败与快速重新开始。
- 暖色调彩色橡皮管动画风格的最小美术集。
- 室内木制风格三层视差背景，所有背景层均可无缝横向循环。
- 键盘控制；游戏手柄作为原型后半段的兼容项。
- 调试显示：受击圈、反弹圈、弹丸类别、当前状态、Boss 阶段、随机种子与实时弹量。

## 3. 设计支柱

### 3.1 躲避始终是常态

普通状态下，普通弹不能被屁股挡住，只能躲避。强化状态只是通过成功反弹特殊弹换来的短暂优势，不能长时间替代躲弹。

### 3.2 反弹必须同时包含空间与时机

特殊弹只有在接触屁股附近的局部判定区，并且玩家处于短暂的主动反弹窗口内，才会被成功反弹。单纯按键不能产生全屏防御。

### 3.3 受击点、控制点和反馈点统一

屁股同时是玩家直接控制的位置、唯一受击判定位置和反弹反馈发生位置。其他身体部位不得影响碰撞结果。

### 3.4 强化是反攻段落，不是自动通关

短期强化会在红色受击核心处零冷却自动反弹所有弹种。玩家仍可在特殊弹抵达核心前，使用更大的主动反弹圈手动反弹并获得能量；任何由强化自动完成的反弹均不产生能量，避免能量自我循环。

## 4. 玩法规则基线

以下数值是首轮调试起点，不是最终平衡结论。所有数值应集中在配置文件中，允许不改代码直接调整。

### 4.1 玩家

- 设计坐标：1280 × 720，按浏览器窗口等比缩放。
- 活动范围：覆盖整个画面；以屁股视觉半径作为边缘内距，保证完整形体不会被画布裁切。
- 初始生命：3。
- 受击核心：以屁股视觉中心为基准的小型圆形碰撞体，初始半径建议 10～14 设计像素。
- 受伤后无敌：初始建议 1 秒，同时清除核心周围的小范围弹丸，避免连续受伤。
- 移动采用直接速度控制和很短的加减速缓动；碰撞核心必须比身体动画更及时。
- 键盘默认：WASD／方向键移动，Z／J 反弹，X／K 技能；键位集中配置。

### 4.2 头部与身体

- 头部无碰撞，在固定的头部锚点活动区内独立平滑游移；屁股移动不直接平移头部活动区，只有最大躯干长度约束可以牵引头部。
- 头部采用阻尼弹簧追踪一个缓慢变化的局部目标，不使用逐帧随机跳动。
- 躯干使用连接头部和骨盆的单块梯形，随两者相对位置整体缩放和旋转；四肢使用简单层级骨骼或约束跟随。
- 两只脚底分别使用固定的场景锚点；腿部连接屁股和脚底，随屁股移动旋转并在活动范围内受控拉伸。
- 躯干需要限制最大拉伸并保持最小厚度；手臂保持骨段长度与首选弯曲方向，腿部保持固定脚底、连续弯曲方向和有限伸缩。
- 身体姿态可以低帧率采样以形成传统动画感，但受击核心和输入保持 60 Hz 更新。

### 4.3 弹丸

- 普通弹：碰到受击核心造成伤害；普通状态下不能被反弹。
- 特殊弹：未反弹时同样造成伤害；成功反弹后沿“反弹圈中心指向首次接触点”的方向自由弹射，并在后续能量批次中增加 1 点能量。
- 特殊弹除颜色外，还必须拥有独特轮廓、脉冲节奏和提示音。
- 反弹弹只在屏幕上下边界镜面弹射，抵达左右边界后直接飞出；只有实际轨迹接触Boss受击区域时才造成伤害。
- 所有高速弹使用扫掠检测，避免穿过小型受击圈或反弹圈。

### 4.4 主动反弹

- 按下反弹键开启短暂局部窗口，首轮建议从 140 ms 开始调试。
- 成功反弹立即恢复动作，并给予停顿、闪光、形变和独立音效。
- 失败按键产生轻微恢复时间，首轮建议从 180 ms 开始调试，防止无脑连按。
- 同一批紧密特殊弹是否允许一次输入连续反弹，应作为显式配置进行 A/B 测试。

### 4.5 能量与短期强化

- 能量范围：0～10 点。
- 每次成功反弹特殊弹增加 1 点。
- 消耗 2 点进入 3.5 秒强化状态。
- 强化期间，普通弹和特殊弹接触红色受击核心时均零冷却自动反弹；同一帧接触的多枚弹丸必须全部处理，自动反弹不增加能量。
- 强化期间，玩家仍可在特殊弹抵达核心前主动按键反弹；手动成功反弹仍增加 1 点能量。
- 受到伤害时结束强化，防止状态表现与受伤反馈相互冲突。

### 4.6 满能量大招

- 满 10 点时允许释放大招；普通强化仍需保持可选，不能因为恰好满能量而擅自改变玩家的短按行为。
- 原型输入方案：短按技能键仍释放 2 点强化；从满能量状态持续按住技能键达到阈值后升级为大招。长按确认期间先提供普通弹反射保护，避免识别长按造成不公平受伤。
- 大招立即停止 Boss 继续生成弹丸，吸收场上所有普通弹和特殊弹，并在演出期间关闭玩家受击。
- 视觉上吸收全部弹丸；逻辑上将弹量聚合成有限数量的反击弹或一次伤害事件。
- 反击弹从多个随机高度向前齐射，整体保持近似水平，仅加入轻微纵向散射；弹幕越多只增加有限的视觉弹数与聚合伤害，不改为自动追踪。
- 伤害随吸收数量提高，但设置上限或递减收益，避免高密度招式反而成为 Boss 的自杀按钮。
- 大招结束后提供短暂安全窗口，再开始下一轮弹幕。

### 4.7 战斗状态优先级

从高到低：大招演出、受伤、主动反弹、强化、普通。任何状态切换都通过单一状态机处理，避免同一帧同时发生受伤、反弹和能量消费。

## 5. Boss 与弹幕内容

原型 Boss 只承担移动、躲避、反弹和能量循环的测试功能。它始终被限制在画面右侧区域内随机移动，每次到达随机目标点或完成短暂停留后再选择下一个目标点，避免逐帧随机造成抖动。

Boss 默认以单发形式朝玩家屁股的当前坐标发射子弹。每一发子弹生成时独立进行一次类型判定：20% 为特殊弹，80% 为普通弹；三个阶段均使用相同概率。测试模式应支持固定随机种子，以便复现相同的移动与特殊弹序列。

### 第一阶段：慢速移动、慢速发射

- Boss 在右侧区域内慢速随机移动。
- 开火频率较低，弹丸飞行速度较慢。
- 初始调试值：移动速度 45 px/s、发射间隔 1,200 ms、弹丸速度 170 px/s。
- 阶段区间：Boss 生命值 100%～67%。

### 第二阶段：较慢移动、较快发射

- Boss 在右侧区域内以较慢速度随机移动。
- 开火频率提高，弹丸飞行速度保持中等。
- 初始调试值：移动速度 65 px/s、发射间隔 700 ms、弹丸速度 200 px/s。
- 阶段区间：Boss 生命值 66%～34%。

### 第三阶段：较快移动、快速弹丸

- Boss 在右侧区域内较快随机移动。
- 保持较快的开火频率，并显著提高弹丸飞行速度。
- 初始调试值：移动速度 120 px/s、发射间隔 700 ms、弹丸速度 320 px/s。
- 阶段区间：Boss 生命值 33%～0%。

Boss 的活动边界初始设为画面横向 72%～92%、纵向 12%～88%，并根据 Boss 视觉尺寸向内收缩。Boss 只受到反弹弹和大招伤害。以上速度与间隔均属于集中配置中的起始值，阶段结构与 20% 特殊弹概率属于固定规则。

## 6. 美术方向

### 6.1 总体风格

- 以 1930 年代彩色橡皮管动画为视觉参考，使用原创角色比例、轮廓、场景与界面语言。
- 形体采用夸张弧线、柔软关节和明显的挤压拉伸。
- 主配色使用奶油色、赭石、陶土橙、芥末黄、暖红和木褐色。
- 允许少量冷色或高纯度洋红作为功能色，以保证普通弹、特殊弹和可交互区域不会融入暖色背景。
- 外轮廓使用深褐或近黑色，避免纯黑大面积压住背景与弹幕。

### 6.2 视觉信息优先级

从高到低：屁股受击核心、危险弹丸与特殊弹、Boss 攻击预告、角色表情、身体与背景装饰。

- 屁股周围需要稳定且克制的受击核心提示，不依赖调试框。
- 屁股使用独立高层级、完整不透明度和强轮廓，角色部位发生重叠时必须显示在头部、躯干和四肢之上；实际参与物理判定的中心小圆使用高纯度暖红色，外围屁股主体使用较浅的暖色，但仍必须比其他表演部位更明显。弹丸层级必须高于外围屁股主体、低于红色受击核心。
- 非碰撞的头部、躯干与四肢统一降低饱和度、对比度和不透明度；子弹绘制在这些表演部位之上，必要时穿过身体产生轻微波纹。
- 特殊弹使用颜色、形状、脉冲和音效四重提示。
- 反弹后的弹丸立即更换颜色、尾迹和音色。
- 头部主要承担注视危险、预备表情、受惊与得意等状态提示。

### 6.3 动画策略

- 游戏逻辑与碰撞以 60 Hz 更新。
- 身体姿态可采用 12～15 fps 的阶梯式视觉采样，模拟手绘动画节奏。
- 原型阶段的骨骼动画交付范围限定为：独立头部锚点有限游移、梯形躯干缩放与旋转、固定脚底锚点、腿部弹性连接、手臂跟随和拉伸限制。
- 基础骨骼先以几何占位图实现；头部活动范围、拖拽目标和脚底锚点只用于开发验证，不进入试玩画面，P7 再替换为最小风格化美术资源。

### 6.4 三层室内木制视差背景

- 远层：木墙、墙板、窗影或远处结构；最低对比度，滚动最慢。
- 中层：立柱、架子、悬挂器物或室内装饰；中等对比度与速度。
- 近层：桌沿、栏杆、帘幕或贴近镜头的木制结构；局部遮挡，滚动最快。
- 每层均制作成可横向无缝拼接的独立图层，左右边缘的纹理、明暗和结构高度必须连续。
- 避免只在单张图中央出现唯一焦点物件，否则循环后会暴露重复周期。
- 拼接边缘保留出血区，并在目标浏览器缩放比例下检查接缝、采样线和亚像素抖动。
- 初始滚动速度可按远／中／近约 1 : 2.5 : 5 配置，并整体保持缓慢，以免与弹幕竞争注意力。

## 7. Web 技术方案

### 7.1 技术栈

- Phaser 4.2.x：渲染、场景、资源加载、输入、相机和声音。
- TypeScript：约束战斗状态、弹丸类型和配置数据。
- Vite：本地开发、热更新与静态构建。
- 游戏页面直接使用 Phaser 管理 Canvas/WebGL 渲染与界面。
- 可选测试工具：Vitest 测试纯逻辑；浏览器自动化只做启动与重新开始烟雾测试。

选择 Phaser 的理由是它已覆盖 Web 游戏需要的场景、输入、音频和资源系统。弹幕碰撞采用轻量定制计算，以获得稳定、可解释的判定。

### 7.2 运行与性能策略

- 逻辑使用固定 60 Hz 步进，渲染跟随浏览器刷新率。
- 使用圆形受击判定与扫掠线段检测，不计算身体多边形碰撞。
- 所有弹丸、命中特效和短命粒子使用对象池。
- 首轮性能目标：桌面 Chrome/Edge、1280 × 720 设计分辨率、场上 300 枚活动弹丸时保持稳定 60 fps。
- 对设备像素比设置上限，避免高 DPI 屏幕无意义增加填充压力。
- 页面失焦时暂停逻辑；音频等待首次用户输入后再解锁。
- 构建产物采用纯静态站点形式部署。

### 7.3 建议目录

```text
src/
  main.ts
  game/
    config.ts
    balance.ts
    scenes/
      BootScene.ts
      PrototypeScene.ts
      UIScene.ts
    entities/
      PlayerRig.ts
      Bullet.ts
      PrototypeBoss.ts
    systems/
      CombatStateMachine.ts
      BulletPool.ts
      CollisionSystem.ts
      PatternDirector.ts
      ParallaxBackground.ts
      FeedbackSystem.ts
    patterns/
      prototypePatterns.ts
    ui/
      Hud.ts
assets/
  character/
  boss/
  bullets/
  background/
  audio/
tests/
  combat/
  patterns/
```

玩法规则、输入、碰撞尺寸、技能持续时间、弹速和发射间隔必须放入 `balance.ts` 或独立数据对象，不允许散落在实体类中。

## 8. 开发阶段与验收门

后续由我按以下九个批次逐一实现和验收。预计总量约为 10～15 个有效开发日；该估算只用于控制原型规模，不替代每批的实际验收结果。

### 批次 P0：工程初始化（预计 0.5～1 日）

当前状态：已完成（2026-09-21）。

本批交付：建立 Vite、TypeScript、Phaser 工程；完成 1280 × 720 设计坐标、浏览器等比缩放、场景入口、集中配置、基础资源目录和调试开关。

本批验收：开发服务器和生产构建均能运行；窗口缩放不改变设计坐标；调试开关可显示版本、帧率和场景状态。

验收记录：

- `npm run typecheck` 通过。
- `npm run build` 通过，Vite 7.3.6 成功生成 `dist/` 静态构建产物。
- `npm run dev -- --host 127.0.0.1` 成功启动本地开发服务器。
- 浏览器实测画布逻辑尺寸始终为 1280 × 720；在 1024 × 600 视口中等比显示为 1024 × 576 并居中，恢复默认视口后保持正确比例。
- F3 可显示和隐藏调试层；调试层正确显示项目版本、Phaser 4.2.1、FPS、`BootScene`、当前批次、逻辑尺寸与显示尺寸。
- 浏览器运行日志中无错误或警告。

### 批次 P1：基础人物骨骼（预计 1～2 日）

当前状态：已完成（2026-09-21）。

本批交付：建立可供后续玩家控制系统驱动的人物骨架；以骨盆、独立头部活动区和固定脚底锚点验证头部有限游移、梯形躯干缩放旋转、腿部弹性连接、手臂跟随和拉伸限制。所有部位使用几何占位图，本批交付范围止于上述结构运动。

本批验收：头部始终位于配置的固定游移范围；梯形躯干始终连接头部与骨盆并随两点距离和方向连续缩放、旋转；脚底锚点保持固定，腿部在骨盆与脚底之间连续旋转和拉伸；手臂保持骨段长度约束；极端姿态下无明显反折、断裂、抖动或无效数值；骨架根节点可直接绑定屁股控制点。

验收记录：

- 建立纯逻辑 `PlayerRigModel` 与 Phaser 显示层 `PlayerRigView`，骨盆锚点可由外部重新绑定，P2 可直接接入玩家控制点。
- 头部采用阻尼跟随与有限局部游移；躯干采用单块梯形并根据头部—骨盆轴实时缩放和旋转；四肢使用带固定弯曲方向的双骨段约束。
- `npm run typecheck` 通过。
- `npm run test:rig` 的 6 项自动测试全部通过，覆盖 30 秒自动游移、梯形躯干对齐及缩放旋转、手臂定长、固定脚底与腿部伸缩、极端目标有限且无无效数值、三个锚点相互独立。
- `npm run build` 通过，成功生成生产构建产物；仅保留 Vite 对 Phaser 主包体积的非阻断提示。
- 浏览器实测可将蓝色调试手柄拖至头部活动框外的两个相反方向；实际头部均保持在限制内，梯形躯干连续缩放和旋转且未翻面或塌缩，四肢连接稳定，调试层显示有限数值且最大实测伸展率为 92.8%。
- 按 R 可从手动目标恢复自动游移；浏览器运行日志中无错误或警告。

### 批次 P2：玩家灰盒移动与受伤（预计 1 日）

当前状态：已完成（2026-09-21）。

本批交付：将 P1 骨架根节点绑定到屁股控制点，实现小型圆形受击体、活动边界、3 点生命、受伤无敌、静态测试弹、失败与快速重开。

本批验收：移动响应直接；碰撞图形与实际判定一致；骨架跟随移动且不影响实际控制；连续受伤受到无敌时间保护；重开后玩家与骨架状态全部复位。

子阶段：

- P2a 屁股移动与三锚点联动：已完成并通过用户验收。实现 WASD／方向键输入、短加减速缓动、全屏活动边界，并将骨盆锚点绑定到屁股控制点；头部活动区和两只脚底保持场景坐标独立。
- P2b 受击与失败循环：已完成。实现屁股中心圆形受击体、3 点生命、1 秒受伤无敌、可重复生成的静态测试弹、失败锁定与 Enter 快速重开。

P2a 验收记录：

- 移动参数集中配置：最高速度 360 px/s、加速度 3,000 px/s²、减速度 3,600 px/s²；屁股中心活动范围扩展为 X 54～1,226、Y 38～682，覆盖完整 1,280 × 720 画面并保留屁股视觉半径作为内距。
- 对角输入经过归一化，不会比水平或垂直移动更快；到达边界后会同时清除朝边界外的速度。
- 屁股移动每帧先更新控制点，再更新三锚点骨架；头部保持在固定活动区独立游移，躯干连接头部与屁股，两条腿的末端始终锁定各自脚底锚点并随距离弹性变形。
- `npm run test:movement` 的 4 项自动测试全部通过，覆盖加速、对角速度、松键减速和四边边界限制；`npm run test:rig` 的 6 项测试全部通过，新增固定脚底和独立锚点覆盖。
- 躯干允许长度提高到 1,500 px，使屁股移动到全屏任一边缘时，固定头部活动区不会被骨盆牵引；全屏四角姿态自动测试保持有限数值，两只脚底漂移均为 0。
- 屁股外围主体使用深度 25、弹丸使用深度 30、红色受击核心使用深度 40，表演身体使用深度 20；弹丸穿过角色时覆盖浅色外围，但会被红色核心覆盖。屁股保持完整不透明度和强轮廓，其他身体部位使用约 42%～55% 不透明度与低饱和配色。
- 浏览器运行日志中无错误或警告；重新载入后正确恢复初始位置。
- 用户已通过 P2a 验收，确认移动范围、图层优先级与三锚点联动可进入后续功能制作。

P2b 验收记录：

- 屁股中心使用半径 12 px 的逻辑圆形受击体，画面中的红色核心圈与实际判定共用同一配置；外围屁股主体改用浅暖色，头部、躯干和四肢不参与碰撞。
- 玩家初始生命为 3；红色核心受击后进入 1 秒无敌，外围主体、轮廓和红色核心组成的整个屁股同步闪烁；无敌期内命中不重复扣血。
- 建立 4 枚半径 18 px 的紫色静态测试弹；造成有效伤害时该弹消失，1.25 秒后重生，可在固定位置重复验证无敌帧。
- 生命归零后进入失败状态，玩家移动锁定并显示重开提示；按 Enter 重启场景后，生命、位置、骨架、子弹和界面全部恢复初始状态。
- 测试结果：`npm run test:combat` 7/7、`npm run test:movement` 4/4、`npm run test:rig` 6/6，`npm run typecheck` 通过；`npm run build` 成功生成生产构建，仅有 Phaser 主包体积的非阻断提示。
- 浏览器实测连续受击按无敌时间间隔结算，生命从 3 递减至 0 后正确锁定；Enter 重开后恢复 3 点生命和 4 枚测试弹，浏览器运行日志中无错误或警告。

### 批次 P3：特殊弹与主动反弹（预计 1～2 日）

当前状态：已完成并通过用户验收（2026-09-22）。

本批交付：在静态发射器环境中实现普通弹、特殊弹、局部反弹圈、反弹有效窗口、失败恢复、接触点自由弹射、上下边界镜面反射和对应调试信息。

本批验收：普通弹在普通状态下无法反弹；特殊弹只有同时满足局部接触和按键窗口才会反弹；成功与失败可被清楚区分；高速弹不穿透判定。

实现与验证记录：

- 建立可复用弹丸池与右侧静态发射器；P3 测试序列交替生成粉色菱形特殊弹和紫色圆形普通弹，两者拥有独立轮廓，特殊弹额外使用脉冲环。
- 特殊弹的逻辑半径调整为 13 px，仅比屁股红色受击核心的 12 px 半径大 1 px；主体脉冲和外围提示环同步收紧，避免视觉范围夸大反弹时机。
- 反弹圈半径为 70 px，`Z`／`J` 开启 140 ms 有效窗口；空挥或被伤害打断后进入 180 ms 恢复。按键使用事件缓冲，极短输入也会在下一逻辑帧处理。
- 成功反弹反馈以实际 70 px 判定圈为起始大小，随后向外扩张至 1.55 倍并同步渐隐、收窄线条，让反馈明确对应刚刚生效的判定范围。
- 普通弹不参与主动反弹查询；特殊弹只在同时接触局部圈且窗口激活时转为反弹弹。成功后取消自动追踪，以“反弹判定圈中心指向首次接触点”的方向按 620 px/s 弹射，使玩家能够通过接触位置主动控制射角。
- 反弹弹只在屏幕上下边界进行镜面弹射，纵向速度反转而横向速度保持不变，最多弹射 3 次；抵达左右边界后保持原方向飞出并回收，不进行左右折返。原型阶段反弹弹不伤害玩家，命中右侧目标后计数并回收。
- 玩家、反弹圈与反弹目标都使用线段—圆形扫掠判定；反弹方向使用扫掠检测返回的首次接触点，弹丸在单帧跨过整个判定圈时仍会按正确方向弹出。墙面弹射前后的分段轨迹也分别参与目标碰撞检测。
- 图层顺序为表演身体 20、浅色屁股外围 25、弹丸 30、红色受击核心 40、反弹圈 45；弹丸会遮挡外围但低于实际受击核心。
- `npm run test:combat` 的 21 项测试全部通过，覆盖生命、无敌、反弹窗口、反馈动画进度、失败恢复、普通弹禁止反弹、首次接触点、自由弹射方向、上下镜面弹射、左右出界、目标命中与高速扫掠命中；骨架 6/6、移动 4/4、类型检查和生产构建均通过。
- 自由弹射与上下边界机制等待浏览器手感验收；右侧静态受击目标继续用于验证直接命中及弹墙后的轨迹可读性。
- 用户验收重点：140 ms 时机的手感、接触位置与出射角是否符合直觉、上下弹射和左右出界是否明确，以及自由轨迹命中右侧目标的可控性。

### 批次 P4：三阶段测试 Boss（预计 1～2 日）

当前状态：基础功能已完成并通过用户验收（2026-09-22）；完整可玩性随P5、P6机制合并评估。

本批交付：实现第 5 节规定的 Boss 右侧活动范围、随机目标移动、单发瞄准射击、生命分段、三阶段参数、固定随机种子、每发 20% 特殊弹判定，以及与Boss身体不同位置对应的多个实际受击区域。

本批验收：Boss 不越出右侧边界；三个生命阶段准确切换；相同随机种子可复现移动与弹种；对足够大的样本统计时特殊弹比例接近 20%；反弹弹不依赖自动追踪，只有实际轨迹接触Boss对应身体位置时才造成伤害。

实现与验证记录：

- 新增独立Boss模型与视图。Boss拥有12点生命，按生命比例在8点和4点时切换第二、第三阶段；移动速度、开火间隔和弹丸速度严格使用第5节集中配置。
- Boss中心活动范围按画面横向72%～92%、纵向12%～88%并结合当前视觉尺寸向内收缩；Boss只朝随机目标点连续移动，到达后停留0.25秒再选择新目标，不进行逐帧随机抖动。
- 移动与弹种分别使用由固定种子 `20260922` 派生的独立随机流，保证调整移动采样不会改变20%特殊弹序列；调试面板显示种子、坐标、阶段、生命、累计弹数与特殊弹数。
- Boss头部、躯干和下部对应三个随Boss移动的实际圆形受击区域。自由反弹弹的分段扫掠轨迹只有接触任一区域才扣除1点Boss生命，命中后弹丸立即回收。
- 弹丸生成改为由Boss发出独立射击事件，生成瞬间记录玩家屁股坐标作为瞄准点；阶段变化只影响后续移动、开火间隔和新生成弹丸的速度。
- 自动化验证共26项战斗测试通过，其中Boss专项覆盖右侧边界、阶段阈值、固定种子复现、2000发样本的特殊弹比例和移动受击区域；骨架6/6、移动4/4、类型检查和生产构建均通过。
- 浏览器运行检查中，Boss从初始位置移动至右侧区域内的新目标点，前5发包含1发特殊弹，调试面板同步显示 `Boss seed 20260922`、阶段1和动态坐标；页面无启动或渲染错误。
- 用户验收重点：三阶段压力差异、Boss移动是否平滑、特殊弹实际频率、自由反弹命中不同身体位置的可控性，以及Boss与调试面板在不同分辨率下的可读性。

### 批次 P5：能量与 2 点强化（预计 1 日）

当前状态：已完成并进入下一批次（2026-09-22）。

本批交付：实现 0～10 点能量、成功反弹特殊弹 +1、消耗 2 点的短期强化、强化计时和 HUD。

本批验收：强化期间所有弹种在红色核心处零冷却自动反弹且不增加能量；特殊弹仍可提前手动反弹并正常增加能量；能量不足时无法释放；受伤会正确结束强化。

实现与验证记录：

- 新增独立玩家能力模型，能量范围为0～10点；每次手动成功反弹特殊弹增加1点，达到上限后不溢出。所有强化自动反弹及反弹弹命中Boss均不增加能量。
- `X`／`K`在能量不少于2点且当前没有强化时消耗2点，启动持续3.5秒的强化；能量不足、强化期间重复输入均不消费能量或刷新持续时间，并在HUD给出对应反馈。
- 强化期间，普通弹与特殊弹只有实际扫掠接触半径12 px的红色受击核心时才自动反弹；反弹没有冷却或恢复期，同一逻辑帧内循环处理全部接触弹丸。反弹方向继续使用“核心中心指向首次接触点”的自由弹射规则，并保留上下镜面弹射。
- 特殊弹在强化期间仍可于70 px局部圈内通过140 ms主动窗口提前手动反弹并获得1点能量；若未手动处理而由强化在红色核心处自动反弹，则不获得能量。
- 玩家受到一次实际扣血时，当前强化立即结束；无敌帧期间被忽略的接触不会重复取消状态。玩家、Boss或场景重开时能力状态随场景恢复初始值。
- HUD新增10格能量显示、强化剩余时间和启动失败反馈；强化生效时屁股外围显示高优先级青色脉冲轮廓，调试面板同步显示能量与强化状态。
- 自动化验证共33项战斗测试通过，其中能力专项覆盖能量上限、能量不足、2点消费、3.5秒持续、重复输入和受伤取消，弹丸专项分别覆盖普通弹与特殊弹的强化自动反弹；骨架6/6、移动4/4、类型检查和生产构建均通过。
- 浏览器运行检查确认P5标题、能量格、`X/K`操作提示、Boss状态与调试信息能同时显示，初始状态为能量0/10且强化关闭；页面无启动或渲染错误。
- 用户验收重点：积攒两点能量所需风险、3.5秒强化的价值、高密度弹幕是否全部连续反弹、特殊弹手动提前反弹与核心自动保底之间的选择，以及受伤中断是否清楚。

### 批次 P6：满能量大招与完整胜负循环（预计 1 日）

当前状态：已形成原型初期试玩版（2026-09-22），等待完整玩法验收。

本批交付：实现满 10 点的大招输入、全场吸收、伤害聚合、演出无敌、安全退场、Boss 击败、胜利与完整重试。

本批验收：短按强化和满能量大招均可被可靠选择；大招期间弹幕生成、碰撞和能量结算不会重复；清场后不会立即受到无预警伤害；一局可从开始完整进行至胜利或失败。

实现与验证记录：

- 满10点能量时，短按`X`／`K`仍消耗2点进入3.5秒强化；持续按住完成2秒施法才升级为大招并消耗全部能量。蓄力期间红色核心会自动反弹普通弹，特殊弹仍需主动处理；受到实际伤害会取消蓄力且不扣除能量。
- 大招依次经过0.85秒吸收、2.9秒连射和0.75秒安全恢复。吸收开始时停止Boss继续发弹；场上全部普通弹、特殊弹和反弹弹不再瞬间消失，而是保留原外观、缩小并沿交错弧线加速收束到玩家屁股核心，到达核心后才回收。吸收、齐射与恢复期间玩家无敌且Boss开火计时冻结，结束后从原剩余计时继续，避免恢复瞬间无预警贴脸出弹。
- 齐射从玩家屁股右侧开始，以0.10秒间隔连续生成8枚基础弹，并按吸收量增加至最多18枚；每发只在屁股相对高度±28 px内随机偏移，不再从全屏随机高度同时生成。所有弹丸以800 px/s大致水平向右飞行，纵向散射限制在±0.06弧度；外形与反弹弹统一为金色圆弹、青色拖尾和深色轮廓。每枚弹丸使用扫掠判定接触Boss身体区域，命中后立即消失并在接触点显示0.28秒扩散闪光。伤害与视觉弹数解耦，使用`4 + floor(sqrt(吸收数量))`的递减公式且最高8点，并在连射演出结束时统一结算，避免重复扣血。
- Boss生命归零后显示“挑战成功”，玩家生命归零显示“挑战失败”，两种结果都可按Enter完整重开；大招状态、弹幕、吸收计数与待结算伤害均随场景重置。
- Boss特殊弹概率在技能专项测试期间曾临时覆盖为80%；初期试玩版已恢复三个阶段共用的正式规则20%。
- 试玩版保留生命、反弹、能量、技能状态和Boss生命等玩家必要HUD；右侧开发参数列表、F3提示、头部蓝色目标圈、头部活动范围框、脚底锚点十字圈及全屏移动边界框均不再绘制，逻辑约束和自动骨骼动画保持不变。
- 场景标题与版本更新为“原型初期试玩版”／`0.9.0-playtest`。
- 自动化验证共45项战斗测试通过，新增覆盖满能量短按分流、2秒长按大招状态机、受伤取消蓄力、蓄力期间仅普通弹保护、弹丸吸入核心的连续动画、Boss暂停开火、从屁股相对高度依次连射、轻微散射、命中Boss后消失与冲击反馈、弹数／伤害上限和固定种子复现；移动4/4、骨架6/6、类型检查和生产构建均通过。
- 浏览器运行检查确认P6标题、操作提示、胜负重开与调试状态正确显示，页面运行日志无错误或警告。用户验收重点：2秒施法时长是否合适、弹丸收束到屁股核心的速度与弧线是否清楚、屁股附近随机高度连射是否形成预期的高密度喷射感，以及0.75秒恢复后重新接战是否自然。

### 批次 P7：最小美术与三层背景（预计 1～2 日）

当前状态：未开始。

本批交付：替换暖色调原创橡皮管角色与弹丸占位图；接入远、中、近三层室内木制背景及无缝横向滚动；完成核心视觉层级。

本批验收：身体穿弹不会被误认为碰撞错误；特殊弹不只依赖颜色也能识别；三层背景连续滚动 5 分钟无可见接缝；弹幕在所有背景位置上保持清楚。

### 批次 P8：反馈、性能与外部试玩（预计 2～3 日）

当前状态：未开始。

本批交付：补齐音效、停顿、震动、形变、HUD 和手柄映射；完成性能剖析、浏览器兼容检查、参数收束及至少 5 名未参与设计的试玩记录。

本批验收：满足第 9 节的可用性、循环、手感和性能标准，并形成原型阶段总结与下一阶段建议。

## 9. 原型成功标准

### 9.1 可用性

- 至少 4/5 的首次试玩者在一局内理解“普通弹要躲、特殊弹可主动反弹”。
- 至少 4/5 能指出屁股是唯一受击点，不把头部或躯干穿弹误认为错误。
- 发生受伤后，至少 80% 的情况玩家能够正确说明自己被什么击中。

### 9.2 循环有效性

- 试玩者会主动调整移动或反弹时机来处理特殊弹，而不是把它当作普通弹一律躲开。
- 不同试玩者自然出现“频繁使用 2 点强化”和“尝试积攒 10 点大招”两种选择。
- 如果绝大多数玩家永远只使用其中一种能力，则先调整消耗、持续时间和弹幕窗口，不增加新系统。
- 玩家能够在失败后立即重新开始，并愿意重复尝试至少三局。

### 9.3 手感与表现

- 输入到受击核心移动之间没有可感知延迟。
- 反弹成功与失败在画面和声音上能够瞬间区分。
- 大招清屏后不会让玩家在无预警情况下重新进入贴脸弹幕。
- 300 枚活动弹丸的压力测试中维持稳定帧率和稳定判定。

## 10. 主要风险与处理顺序

### 风险一：强化再次消灭躲弹

优先缩短持续时间或提高消耗，不先增加不可反弹弹种。只有在数值无法解决时才引入新的攻击规则。

### 风险二：特殊弹变成免费能量或不值得接

保持 20% 生成概率不变，优先调整反弹窗口、失败恢复、弹丸速度和开火间隔，使反弹既有风险又可稳定学习。

### 风险三：巨大非碰撞身体遮挡弹幕

先调整图层、对比度和姿态，再考虑缩小身体。身体美术必须服务可读性。

### 风险四：大招伤害随弹量失控

将视觉吸收数量与实际伤害公式解耦，对伤害使用上限或递减曲线。

### 风险五：程序骨骼动作僵硬或失真

限制自由度，保留少量手工关键姿态；不通过增加更多骨骼解决表演问题。

## 11. 阶段结束决策

完成批次 P8 后，我依据验收证据做一次核心判断：

- 若躲避、主动反弹和能量选择均成立，建议进入首个正式 Boss 与正式美术制作。
- 若躲避成立但能量选择单一，只调整资源经济和弹幕窗口。
- 若主动反弹始终打断移动或无法读懂，优先改变输入与判定，不增加新技能。
- 若身体持续造成碰撞误读，则重新设计角色的实体／非实体视觉关系，再决定内容扩展方式。

## 12. 技术依据

- Phaser 当前官方稳定下载页：<https://phaser.io/download>
- Phaser 官方安装与 TypeScript 说明：<https://docs.phaser.io/phaser/getting-started/installation>
- Phaser 官方输入系统说明：<https://docs.phaser.io/phaser/concepts/input>
- Phaser 官方音频系统说明：<https://docs.phaser.io/phaser/concepts/audio>
- Vite 官方入门文档：<https://vite.dev/guide/>
